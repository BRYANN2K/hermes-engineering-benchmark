#!/usr/bin/env bash
set -euo pipefail
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)
sandbox="$root/runtime/sandbox"
cc -O2 -Wall -Wextra -Werror -o "$sandbox/landlock-run" "$sandbox/landlock-run.c"
printf 'built %s\n' "$sandbox/landlock-run"
