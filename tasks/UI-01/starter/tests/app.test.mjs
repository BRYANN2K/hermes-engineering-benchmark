import test from 'node:test'; import assert from 'node:assert/strict'; import {createRovingTabs} from '../app.mjs';
class El { constructor(){this.attrs={};this.hidden=false;this.disabled=false;this.listeners={};this.focused=0} setAttribute(k,v){this.attrs[k]=String(v)} addEventListener(k,f){(this.listeners[k]??=[]).push(f)} removeEventListener(k,f){this.listeners[k]=(this.listeners[k]??[]).filter(x=>x!==f)} emit(k,e={}){for(const f of this.listeners[k]??[])f({...e,currentTarget:this})} focus(){this.focused++} }
const key=(value)=>({key:value,preventDefault(){this.prevented=true}});
test('initializes and navigates right',()=>{const t=[new El(),new El(),new El()],p=[new El(),new El(),new El()];const c=createRovingTabs(t,p);assert.equal(c.activeIndex,0);assert.equal(t[0].attrs['aria-selected'],'true');const e=key('ArrowRight');t[0].emit('keydown',e);assert.equal(c.activeIndex,1);assert.equal(t[1].focused,1);assert.equal(p[1].hidden,false);});
test('click and select activate tabs',()=>{const t=[new El(),new El()],p=[new El(),new El()];const c=createRovingTabs(t,p);t[1].emit('click');assert.equal(c.activeIndex,1);assert.equal(c.select(0),true);assert.equal(c.activeIndex,0);assert.equal(c.select(9),false);});
