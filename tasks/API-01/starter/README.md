# Implement conditional document writes with ETags

Starter repository for benchmark task `API-01`. Read `../task.md` for the full observable contract.

Run the public suite:

```bash
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```
