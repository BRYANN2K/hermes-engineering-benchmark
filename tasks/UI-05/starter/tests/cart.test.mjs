import test from 'node:test';import assert from 'node:assert/strict';import {createCart} from '../cart.mjs';function fx(seed=null){const data=new Map(seed?[['checkout-cart',seed]]:[]),renders=[];return {storage:{getItem:k=>data.get(k)??null,setItem:(k,v)=>data.set(k,v)},view:{render:x=>renders.push(x)},data,renders}}test('adds, totals and persists',()=>{const f=fx(),c=createCart(f);assert.equal(c.add({id:'p1',name:'Lamp',priceCents:3000,stock:3}),true);c.add({id:'p1',name:'Lamp',priceCents:3000,stock:3});assert.equal(c.snapshot().itemCount,2);assert.equal(c.snapshot().subtotalCents,6000);assert.equal(c.snapshot().shippingCents,799);assert.ok(f.data.has('checkout-cart'));});test('quantity zero removes',()=>{const f=fx(),c=createCart(f);c.add({id:'x',name:'X',priceCents:10,stock:2});assert.equal(c.setQuantity('x',0),true);assert.equal(c.snapshot().items.length,0);});
