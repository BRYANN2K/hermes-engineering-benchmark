import test from 'node:test';import assert from 'node:assert/strict';import {createAutocomplete} from '../autocomplete.mjs';
function fixture(){let jobs=new Map(),next=1;const scheduler={setTimeout(fn){const id=next++;jobs.set(id,fn);return id},clearTimeout(id){jobs.delete(id)},run(){for(const [id,fn] of [...jobs]){jobs.delete(id);fn()}}};const calls=[];const view={items:null,loading:false,error:'old',render(x){this.items=x},setLoading(x){this.loading=x},setError(x){this.error=x}};return {scheduler,view,calls,jobs};}
test('debounces to the latest trimmed query',async()=>{const f=fixture();const c=createAutocomplete({scheduler:f.scheduler,view:f.view,fetchSuggestions:async q=>{f.calls.push(q);return [{id:'1',label:q}]}});c.input(' a ');c.input(' ab ');assert.equal(f.jobs.size,1);f.scheduler.run();await new Promise(resolve=>setImmediate(resolve));assert.deepEqual(f.calls,['ab']);assert.equal(f.view.items[0].label,'ab');assert.equal(f.view.loading,false);});
test('empty input clears without fetching',()=>{const f=fixture();const c=createAutocomplete({scheduler:f.scheduler,view:f.view,fetchSuggestions:async()=>[]});c.input('   ');assert.deepEqual(f.view.items,[]);assert.equal(f.jobs.size,0);assert.equal(f.view.error,null);});
