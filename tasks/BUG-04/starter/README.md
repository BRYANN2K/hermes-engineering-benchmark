# Fix streaming UTF-8 NDJSON decoding

Starter repository for benchmark task `BUG-04`. Read `../task.md` for the full observable contract.

Run the public suite:

```bash
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```
