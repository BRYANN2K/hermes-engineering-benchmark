# Implement dependency batch partitioning

Starter repository for benchmark task `FEAT-03`. Read `../task.md` for the full observable contract.

Run the public suite:

```bash
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```
