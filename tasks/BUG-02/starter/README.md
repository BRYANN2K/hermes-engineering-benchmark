# Fix immutable deep configuration merge

Starter repository for benchmark task `BUG-02`. Read `../task.md` for the full observable contract.

Run the public suite:

```bash
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```
