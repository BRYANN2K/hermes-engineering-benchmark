# Correct per-label backup retention selection

Starter repository for benchmark task `BUG-05`. Read `../task.md` for the full observable contract.

Run the public suite:

```bash
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```
