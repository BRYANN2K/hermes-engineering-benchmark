import test from 'node:test';import assert from 'node:assert/strict';import {createModalManager} from '../modal.mjs';class E{constructor(){this.hidden=true;this.attrs={};this.listeners={};this.focused=0;this.isConnected=true}addEventListener(k,f){(this.listeners[k]??=[]).push(f)}removeEventListener(k,f){this.listeners[k]=(this.listeners[k]??[]).filter(x=>x!==f)}emit(k,e={}){for(const f of this.listeners[k]??[])f(e)}setAttribute(k,v){this.attrs[k]=v}removeAttribute(k){delete this.attrs[k]}focus(){this.focused++}}
test('opens and closes from controls',()=>{const d=new E(),o=new E(),a=new E(),doc={activeElement:o};const m=createModalManager({dialog:d,opener:o,focusables:[a],documentLike:doc});o.emit('click');assert.equal(m.isOpen,true);assert.equal(d.hidden,false);assert.equal(a.focused,1);d.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(m.isOpen,false);assert.equal(o.focused,1);});
